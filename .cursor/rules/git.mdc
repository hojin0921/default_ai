---
description: Git workflow rules
alwaysApply: true
---

# Git

- 작업 시작 전 git 상태를 확인한다.
- 사용자가 만든 기존 변경사항을 덮어쓰거나 임의로 되돌리지 않는다.
- 관련 없는 파일을 commit하지 않는다. 시크릿(`.env` 등)을 commit하지 않는다.
- force push, hard reset, branch 삭제를 임의로 실행하지 않는다.
- 커밋·푸시는 사용자가 요청할 때만 한다. 요청 시 변경 내용을 먼저 확인한다.
