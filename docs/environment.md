# Environment Variables

실제 secret 값은 이 문서에 **기록하지 않는다**. `.env.example`과 이름을 맞춘다.

| Variable | Required | Description |
|----------|----------|-------------|
| <!-- TODO --> | Yes/No | <!-- 설명만. 값 금지 --> |

## Local Setup

<!-- TODO: `.env.example`을 복사해 `.env`를 만드는 절차 -->

## Security

- `.env`는 커밋하지 않는다.
- AI/문서/로그에 실제 비밀값을 붙이지 않는다.
